// to calculate the simple interest
#include<stdio.h>

int main(){
    int p=100, r=4, y=1;
    int si = (p * r * y)/100;
    printf("The value of simple Interest is %d", si);
    return 0;
}
