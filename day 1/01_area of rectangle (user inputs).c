//to calculate the area of rectangle by using
//inputs supplied by the user

#include<stdio.h>

int main()
{
	int l, b;
	printf("what is the l of rectangle\n");
	scanf("%d", &l);
	
	printf("what is the b of rectangle\n ");
	scanf("%d", &b);
	
	printf("the area of a rectangle is %d", l*b);
	return 0;
}
